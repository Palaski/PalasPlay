name: Build PalasPlay APK

on:
  workflow_dispatch:
  push:
    branches: [master, main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install CMake 4.1.2
        run: |
          python3 -m pip install cmake==4.1.2
          cmake --version

      - name: Increase Gradle memory
        run: sed -i 's/-Xmx1536m/-Xmx6g/' gradle.properties

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build universal APKs
        env:
          NO_GS: 'true'
        run: >
          ./gradlew
          :fermata:packageAutoReleaseUniversalApk
          -PABI=arm64-v8a

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: palasplay-apk
          path: fermata/build/outputs/**/*.apk
